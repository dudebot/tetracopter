{
  "name": "tetracopter",
  "version": "1.0.0",
  "description": "Physics simulator and controller for tetrahedral multirotor drone with reversible motors",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && cd dist && git init && git add . && git commit -m 'Deploy' && git remote add origin https://github.com/dudebot/tetracopter.git && git push -f origin HEAD:gh-pages && cd .. && rm -rf dist/.git"
  },
  "devDependencies": {
    "@types/three": "^0.160.0",
    "typescript": "^5.3.3",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.160.0"
  }
}
